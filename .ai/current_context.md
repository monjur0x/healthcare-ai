# Current Context

## Project state (as of 2026-09-10)

Federated multi-agent healthcare CDS framework (proposal: `ai-automation-research.md`). Backend 309 tests + frontend 58 tests pass; `ruff` clean. Two recent commits on `main`/`origin`:

- `6b75585` — restored the deleted test suite + `scripts/baseline_study.py`; fixed `metrics._output_of` (empty-output over-count) and `crew._parse_report` (missing `@staticmethod`).
- `4cef96e` — fixed kidney label inversion in baseline study (`split_dataset` now passes `preset=preset`); added orientation regression test; refreshed `docs/BASELINE_STUDY_RESULTS.md`.

Working tree clean except untracked `dataset` symlink (keep out of commits).

## Entry points

- READ THIS FIRST for the fix list: `.ai/NEXT_TASK.md` (self-contained, prioritized).
- Detailed P0/P1/P2 audit (partly fixed, partly stale): `.ai/backlog.md`.
- Proposal: `ai-automation-research.md` · Architecture: `docs/SOFTWARE_ARCHITECTURE.md` · Decisions: `docs/DECISIONS.md` · Privacy honesty: `docs/PRIVACY_NOTES.md`.

## Environment

- Python venv: `backend/CrewAI/.venv-opencode/bin/python` (has torch/opacus/flwr/crewai/streamlit/chromadb/pytest).
- Ruff: `~/.local/bin/ruff`.
- Datasets live at `/home/monjur0x0/dataset` (symlinked as `dataset/`; real preset files: `diabetes.csv`, `heart_disease_uci.csv`, `kidney_disease.csv`, `sepsis_icu_synthetic.csv`). NOT committed.
- Backend start: `cd backend && DATASET_DIR=~/dataset ./CrewAI/.venv-opencode/bin/python -m uvicorn api.main:app --port 8000`. n8n via `scripts/start_demo.py` or Docker.
- Baseline study (run from `backend/`): `DATASET_DIR=~/dataset ./CrewAI/.venv-opencode/bin/python scripts/baseline_study.py` (regenerates `docs/BASELINE_STUDY_RESULTS.md` tables; preserves hand-written Findings section).
- Backend suite from `backend/`: `./CrewAI/.venv-opencode/bin/python -m pytest`. Frontend from `frontend/`: same python `-m pytest dashboard/tests/`.

## Verified live

- Kidney baseline bug fixed: study split now matches training orientation (kidney 0.980 acc / 1.000 ROC across all baselines).
- RQ1/RQ2/RQ3 answers in `docs/BASELINE_STUDY_RESULTS.md` Findings (regenerated tables match hand-written section).
- Frontend smoke tests updated for the 7-tab app (keyed widgets + session-state feature inputs).
- P2.1 readmission DONE 2026-09-11 (ADR-018): `train_outcome` + route `outcome` branch + `report.readmission` + leakage exclusion (sepsis 75→74 feats, study regen); mortality data-blocked. 352 tests pass.
- P2.2 explainability DONE 2026-09-11 (ADR-019): `explain.py` (SHAP tabular + Grad-CAM), per-preset background persist/match, Agent 5 method tags, `shap_driven` route flag, labeled heuristic fallback. 371 backend tests pass, ruff clean.
- P2.3 treatments DONE 2026-09-11 (ADR-020): `treatments.py` grading (evidence overlap + SHAP drivers), inline citations / playbook-only labels, shared Agent 4/5 attribution, `graded` + `evidence_grounded` route fields. 381 backend tests pass, ruff clean.
- P2.4 n8n query DONE 2026-09-11 (ADR-021): disease-aware Build RAG Query node (node-verified JS), evidence-retrieval honors caller query, predictor exposes disease/label. 387 backend tests pass, ruff clean.
- P2.5 multi-agent claim DONE 2026-09-11 (ADR-022): single-agent LLM polish (one agent/task/kickoff, base values preserved) + 7 deterministic traced stages; README/architecture/proposal §6 re-scoped; pin test locks stage names/order.
- Cleanup DONE 2026-09-12: cleared `__pycache__`/`.pytest_cache`/`.ruff_cache`; removed empty untracked `models/multimodal/` + `preprocessing/multimodal/` placeholders; fixed `orchestrator/__init__.py` missing `LLMNotConfiguredError` re-export (was in `__all__` but unimported). 388 backend tests pass, ruff clean.
- Demo-rehearsal fix DONE 2026-09-12: `start_demo.py` imported + activated n8n workflows AFTER server boot, so production webhooks never registered → dashboard CSV upload failed with generic "Backend request failed". Added `restart_n8n_for_workflows()` (restart after import). Verified: `demo4_diabetes.csv` via webhook → Diabetes 85.3% HIGH risk. Note: CSV upload requires the matching preset model served (dashboard trains it when an assessment type is selected); with `multi_disease` served, Pima-8 CSVs correctly 422 (canonical 11 required).
- v2 workflow JS typo FIXED 2026-09-12: `n8n/clinical-full-v2.json` "Assemble Report" node had `json||{}}]` (missing paren + brace) → every v2 execution died with `SyntaxError: Unexpected token ']'` and webhook returned 200-empty. Fixed to `json||{})}}]`, added `test_code_nodes_are_valid_javascript` regression test (node --check over all Code nodes; first version only checked the wrapper string and would have missed it — now embeds raw source in a probe function). Deployed into live n8n (entity + active history version both patched, n8n restarted). Verified: healthy → success/low-risk; critical → success/high-risk, notified=True, stored=True. Backend re-serving `multi_disease` (canonical 11) after diabetes retrain had replaced it.
- Train-scope routing bug FIXED 2026-09-12: end-to-end `422 Prediction requires feature row` on every CSV run that retrained first. Root cause: "HTTP: Train Model" → "IF: CSV Input?" edge fed the train *response* into the IF node, so `!!$json.body.csv_b64` was always false after training → fell through to the plain-patient branch with dashboard's `features:{}`. Fixed `n8n/healthcare-endtoend.json` IF condition to `!!$('Webhook: End-to-End Pipeline').first().json.body.csv_b64` (matches the webhook-scoped pattern "HTTP: Analyze CSV" already used); added `test_branch_after_train_uses_webhook_scope` + generic `test_no_if_node_trusts_http_input_scope` (both negative-controlled against the old condition); deployed to live entity + active history, n8n restarted. Verified: train+CSV in one run → `train 200` then `analyze/csv 200` → Diabetes 85.3% HIGH. 391 backend tests pass, ruff clean. Note: backend currently serving `diabetes` (verification retrain replaced `multi_disease`); dashboard auto-trains per assessment so either state self-heals.
- "n8n executes nothing" diagnosis 2026-09-12: no credential needed (local n8n, no auth; webhooks open). n8n WAS executing — 33 `success` executions in DB (end-to-end runs, feedback-retrain, Risk Monitoring polling ~every 5 min). User was watching v2, which was genuinely empty because only the dashboard 🟢/🔴/⛔ demo buttons fire it. Fired the 🟢 probe manually → first attempt errored at disease-predictor 422 because backend was serving `diabetes` (one-model-at-a-time serving state), NOT a workflow bug; restarted backend with demo-boot env (`API_MODEL_PATH`=multi_disease artifact) → v2 execution #33 success/stored. Backend now on `multi_disease` (matches Monday demo order: v2 segment first, CSV segment trains diabetes itself).
- Full-stack reboot + 4-disease sweep DONE 2026-09-12: /tmp was wiped so n8n DB was gone; relaunched everything via `start_demo.py` (all 4 workflows active, webhooks registered). Fixed dashboard crash on empty n8n responses: `client.webhook_raw`/`_post_n8n_webhook` called `response.json()` unguarded → raw JSONDecodeError killed the page (user's 🔴 traceback); added `_decode_json` helper raising HealthcareAPIError, +2 frontend tests. Root-caused that 🔴 empty-body to serving-state (diabetes served at the time), not a workflow bug. CSV mode verified for all 4 presets (train+analyze, Diabetes 85.3%/heart/kidney/sepsis all success with disease enrichment at `report.prediction.disease`). Manual mode: diabetes/heart/kidney success; sepsis manual 422 `['bmi_primitive'] not in index` → REAL backend bug: scaler fitted on engineered frame, model on selected raw frame, manual path never re-derived engineered cols. Fixed in `orchestrator.services.run_prediction` (CSVFeatureEngineer re-derivation when scaler columns missing) + regression test (negative-controlled: fails pre-fix). Verified sepsis manual e2e → success/sepsis/label-0. 392 backend + 60 frontend pass, ruff clean. Final state: backend on `multi_disease`; v2 🟢 success/low/stored + 🔴 success/high/stored/notified=True (notify log confirms). Known limitation (not fixed, by design): manual single-row path is floats-only end-to-end (API schema → tool), so kidney/sepsis categoricals need pre-encoded numbers; CSV path is the designated raw-data route and works for all 4.
- v2 per-disease sweep DONE 2026-09-12: v2's predictor forwards patient/features/markers but NO preset (uses whichever model is served; Store Report reads body.preset only for namespacing). Trained each preset then fired v2 with matching native features: diabetes/heart/kidney/sepsis all success+stored (high-risk pair notified=True). Evidence excerpts verified disease-specific per stored report (WHO Diabetes / Hypertension-Cardio / CKD / CDC Sepsis). Found + fixed enrichment gap: Assemble Report rebuilt `prediction` with only class/probabilities/confidence, dropping predictor's `disease`/`predicted_label` (end-to-end has them; e.g. "sepsis"/"No Sepsis"). One-line jsCode fix + `test_assemble_report_forwards_prediction_enrichment` contract test; deployed live (entity+history), n8n restarted; stored sepsis report now carries disease+label. 393 backend + 60 frontend pass, ruff clean. Final state: backend restarted to `multi_disease`; 🟢 re-verified (success/low/stored; disease '' + label '0' is by-design for multi_disease, no registry entry).
