# Healthcare AI Backend - Dependencies
# Python 3.11+ required

# Core (always required)
numpy>=2.0.0
pandas>=2.2.0
pydantic-settings>=2.7.0
Pillow>=11.0.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Machine Learning
scikit-learn>=1.6.0
torch>=2.2.0

# Retrieval (RAG) - core
sentence-transformers>=3.0.0

# Federated Learning
flwr>=1.33.0

# Differential privacy
opacus>=1.5.0

# Development
pytest>=8.3.0

# Optional: Multi-agent orchestration (CrewAI)
# Verified working on Python 3.14 via --ignore-requires-python (the
# <3.14 upper bound is metadata-only) plus chromadb>=1.5 (1.1.x fails
# to import on 3.14):
# pip install --ignore-requires-python "crewai>=1.15.0" "chromadb==1.5.9"
# crewai>=1.15.0

# Optional: Persistent vector store (ChromaDB)
# Not compatible with Python 3.13+ yet; install separately if needed:
# pip install "chromadb>=0.5.0"
# chromadb>=0.5.0

# Optional: DICOM image support
# pydicom>=2.4.0
